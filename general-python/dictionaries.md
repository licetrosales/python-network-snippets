# 🗂️ Python Dictionary Quick Summary

## 🔧 Common Methods

- `clear()` – Remove all items  
- `copy()` – Shallow copy  
- `items()` – View (key, value) pairs  
- `keys()` – View of keys  
- `values()` – View of values  
- `pop(key[, d])` – Remove key and return value  
- `popitem()` – Remove and return random pair  
- `update()` – Merge another dict

---

## 🧮 Useful Functions

- `all()` – True if all keys are true or dict is empty  
- `any()` – True if any key is true  
- `len()` – Number of items  
- `sorted()` – Sorted list of keys  
- `type()` – Type check  
- `str()` – String representation

> ⚠️ `cmp()` was removed in Python 3

---

## 🔑 Key Rules

1. **Unique**: Last duplicate key wins  
2. **Immutable**: Use `int`, `str`, `tuple`; not `list` or `dict`
